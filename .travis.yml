language: ruby
install: true
install_trash:
 - bundle install --gemfile=test/dummy/Gemfile
script:
 - cd test/dummy && bundle install --gemfile=./Gemfile --path vendor/bundle
 - cd test/dummy && bundle exec rspec spec -f documentation
